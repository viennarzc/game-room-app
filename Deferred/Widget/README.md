# Deferred widget

The widget is intentionally excluded from active build targets for v1. Before
shipping it, add an App Group, define the shared SwiftData read contract, and
replace the placeholder with a recent game or moment selected from real data.
